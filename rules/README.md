# Compatibility mirror

These files preserve the original `rules/*.list` raw URLs used by deployed
Surge profiles. They are generated from `surge/rules/` by
`tools/build_rules.py`; do not edit them directly.
